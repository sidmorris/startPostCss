body {
  font: OpenSans, system-ui;
  font-weight: normal;
  font-size: 16px;
  font-family: "Open Sans";
  color: var(--color-main);
}

.icon {
  display: inline-block;

  fill: currentColor;
  size: 32px;
  stroke: currentColor;

/* width: 1em;
  height: 1em; */
  stroke-width: 1;
}
